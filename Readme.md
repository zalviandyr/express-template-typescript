# Template Typescript for Express JS

## Installation

- Install dependencies
  ```bash
  > npm i
  ```
- Build server
  ```bash
  > npm run build
  ```
- Run server
  ```bash
  > npm start
  ```
- Server listening on `http://127.0.0.1:4000`
